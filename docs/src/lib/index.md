# Library Functions

Adios provides various library functions to help with writing modules and ensuring consistent & performant merging for
options across uses of adios. These are accessed under the `adios` attrset at `adios.lib`.
